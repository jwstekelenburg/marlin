# Crawl / LM queue weights by LLM category (lowercase exact labels).
# Higher = fetched and summarized sooner. Unlisted categories use `default`.
# Restart fetcher + worker after edits. Links are enqueued only after LM classifies
# the page that contained them — not dropped, just ordered.
#
# seed     = ingest / spider seed hosts
# default  = unknown / unlisted category
# negative = still crawled eventually, after everything better

seed 50
default 0

# demote — homepages are still summarized; outbound links trickle in later
corporate -20
documentation -20
ecommerce -15
saas -10
news -8
social-media -8
government -5
education -5
entertainment -5
parked -30

# boost
personal 20
blog 20
wiki 15
forum 10
